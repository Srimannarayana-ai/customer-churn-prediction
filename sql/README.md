# SQL analysis

Load the cleaned customer table into SQLite and print the queries:

```bash
python scripts/run_sql_demo.py
```

File:
- `churn_analysis.sql` — roster KPIs, contract / tech-support rollups, focus-list mix, windowed call list, tenure bands

The demo script creates `sql/customer_churn.db` locally (gitignored).
